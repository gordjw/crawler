const retrieve = require('./retrieve')

test('returns a document (HTML) string given a URL', async () => {

    expect(true)
    // expect(retrieve('https://github.com/')).toBe('<a href="https://github.com/path">Some link name</a><a href="https://gordjw.me/">Another link</a>')
    
});